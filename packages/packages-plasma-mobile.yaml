{{- $architecture := or .architecture "arm64" -}}

architecture: {{ $architecture }}

actions:
  - action: apt
    recommends: true
    description: Install plasma-mobile desktop
    packages:
      - plasma-mobile-full
      - plasma-mobile-phone
      - plasma-camera
      - plasma-keyboard
      - plasma-discover*
      - gdm3
