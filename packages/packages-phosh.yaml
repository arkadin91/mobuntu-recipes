{{- $architecture := or .architecture "arm64" -}}

architecture: {{ $architecture }}

actions:
  - action: apt
    recommends: true
    description: Install phosh desktop
    packages:
      - phosh-full
      - gdm3
      - gnome-software-plugin-deb
      - gnome-software-plugin-snap
      - yaru-theme-*
